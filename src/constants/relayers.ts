import { ethers } from "ethers";

export const Relayers = {
    devnet: "link-to-be-added",
    testnet: "link-to-be-added",
    mainnet: "link-to-be-added"
};

export const THRESHOLD = ethers.parseEther("0.1");
export const TOP_UP_VALUE = ethers.parseEther("0.1");